// Empty module stub. Used by next.config.mjs to satisfy bundler imports
// of modules that are not meaningful in a browser context (e.g.
// `@react-native-async-storage/async-storage`, `pino-pretty`). Anything
// imported from this stub is `undefined`; the importers either feature-
// detect or only call the methods in a non-browser branch.
export {};
